apiVersion: v1
kind: Service
metadata:
  name: kubi-metrics
  namespace: kube-system
  labels:
    app: kubi-operator
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 8002
      targetPort: 8002
      protocol: TCP
  selector:
    app: kubi-operator

